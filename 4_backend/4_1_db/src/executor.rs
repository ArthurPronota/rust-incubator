use std::env::Args;

use crate::args::{self, Commands} ;
use anyhow::Result ;

/// Выполнить действие из аргументов командной строки
pub async fn execute_command(arg_in: &Args) ->Result<()>{

    Ok(())
}